---
title: "minimax-h3-flf-async"
description: "视频 · `minimax-h3-flf-async`"
---

| 字段 | 值 |
| --- | --- |
| 系列 | 视频系列 |
| model | `minimax-h3-flf-async` |

## 接口

| 方法 | 路径 |
| --- | --- |
| `POST` | `/v1/video/generations` |
| `GET` | `/v1/video/generations/{task_id}` |

Base：`https://api.omnimux.ai` · `sk-` Bearer

## 请求示例

<CodeGroup>
```bash cURL
curl --request POST \
  --url https://api.omnimux.ai/v1/video/generations \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
  "model": "minimax-h3-flf-async",
  "prompt": "slow pan across a product on a table"
}'
```
</CodeGroup>

## 响应示例

<Tabs>
  <Tab title="200">
```json
{
  "task_id": "task_01HXYZABCDEF",
  "status": "queued"
}
```
  </Tab>
  <Tab title="400">
```json
{
  "error": { "message": "Invalid request", "type": "invalid_request_error", "code": "bad_request" }
}
```
  </Tab>
  <Tab title="401">
```json
{
  "error": { "message": "Invalid token", "type": "authentication_error", "code": "unauthorized" }
}
```
  </Tab>
  <Tab title="403">
```json
{
  "error": { "message": "Model not allowed", "type": "permission_error", "code": "forbidden" }
}
```
  </Tab>
  <Tab title="429">
```json
{
  "error": { "message": "Rate limit exceeded", "type": "rate_limit_error", "code": "rate_limit_exceeded" }
}
```
  </Tab>
  <Tab title="500">
```json
{
  "error": { "message": "Internal server error", "type": "server_error", "code": "internal_error" }
}
```
  </Tab>
</Tabs>

成功创建后请轮询 `GET /v1/video/generations/{task_id}`（见 [查询视频任务](/cn/api-reference/tasks/video-task)）。
