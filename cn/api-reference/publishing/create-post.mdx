---
title: "创建帖子"
sidebarTitle: "创建帖子"
description: "创建帖子"
api: "POST https://omnimux.ai/api/social/v1/posts"
---

- 社媒 **发布** 用户 API（非 `sk-` 社交数据读）
- Base：`https://omnimux.ai`；鉴权 access token + `New-Api-User`
- 连接账户命名对齐 Zernio **Connecting Accounts**

## 身份

| 字段 | 值 |
| --- | --- |
| 系列 | 社媒发布 |
| 能力 | 创建帖子 |

## 接口

| 方法 | 路径 |
| --- | --- |
| `POST` | `/api/social/v1/posts` |

Base URL：`https://omnimux.ai`

## 鉴权

| 名称 | 位置 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
| `Authorization` | header | string | 是 | `Bearer <access_token>`（用户 access token） |
| `New-Api-User` | header | string | 是 | 当前用户 id |

## 请求体

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `account_ids` | array | 是 | 已连接账户 id 列表 |
| `text` | string | 否 | 帖子文案 |
| `media_items` | array | 否 | 媒体项（含 `url` 等） |

## 响应

### 200

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `data` | object | 业务数据 |

错误见右栏与 [错误码](/cn/api-reference/errors)。

<Panel>

<RequestExample>

```bash cURL
curl --request POST \
  --url https://omnimux.ai/api/social/v1/posts \
  --header 'Authorization: Bearer <access_token>' \
  --header 'New-Api-User: <user_id>' \
  --header 'Content-Type: application/json' \
  --data '{"account_ids":[1],"text":"hello","media_items":[{"url":"https://example.com/a.png"}]}'
```

</RequestExample>

<ResponseExample>

```json 200
{"success":true,"data":{"id":1,"status":"scheduled"}}
```

```json 401
{
  "success": false,
  "message": "Unauthorized"
}
```

```json 402
{
  "success": false,
  "message": "Insufficient quota. Please top up your account."
}
```

```json 403
{
  "success": false,
  "message": "Forbidden"
}
```

```json 404
{
  "success": false,
  "message": "Not found"
}
```

```json 429
{
  "success": false,
  "message": "Rate limit exceeded"
}
```

```json 500
{
  "success": false,
  "message": "Internal server error"
}
```

</ResponseExample>

</Panel>
