---
title: "kimi-k2.7-code"
description: "Kimi · model `kimi-k2.7-code` · 语言系列"
---

| 字段 | 值 |
| --- | --- |
| 系列 | 语言系列 |
| 品牌 | Kimi |
| model | `kimi-k2.7-code` |

## 调用

- Base URL：`https://api.omnimux.ai`
- 鉴权：`Authorization: Bearer sk-...`
- 可用性以 `GET /v1/models` 与控制台为准（本页为目录快照）

## 协议

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/v1/chat/completions` | OpenAI Chat 兼容 |
| `POST` | `/v1/messages` | Claude Messages（Claude 品牌优先） |
| `POST` | `/v1/responses` | OpenAI Responses |
| `GET` | `/v1/models` | 模型列表 |

```bash
curl -sS https://api.omnimux.ai/v1/chat/completions \
  -H "Authorization: Bearer $OMNIMUX_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"kimi-k2.7-code","messages":[{"role":"user","content":"Hello"}]}'
```

> 图像/视频请改用对应 `/v1/images/*` 或 `/v1/video/generations` 路径与字段。
