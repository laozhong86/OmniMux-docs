---
title: "GLM · 完整参数"
sidebarTitle: "GLM · 完整参数"
description: "GLM · Chat Completions 完整参数（同合同，model 枚举）"
openapi: "openapi/ops/chat/glm.json POST /v1/chat/completions"
---

> - 协议：OpenAI Chat Completions（`POST /v1/chat/completions`）
> - 本页为 **GLM 共用合同**；换模型只改 body 的 `model`
> - 默认同步；`stream: true` 流式

## 可用 model

| model id |
| --- |
| `glm-5.1` |
| `glm-5.2` |


Base URL：`https://api.omnimux.ai`
