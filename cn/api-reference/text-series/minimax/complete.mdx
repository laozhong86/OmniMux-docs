---
title: "MiniMax · 完整参数"
sidebarTitle: "MiniMax · 完整参数"
description: "MiniMax · Chat Completions 完整参数（同合同，model 枚举）"
openapi: "openapi/ops/chat/minimax.json POST /v1/chat/completions"
---

> - 协议：OpenAI Chat Completions（`POST /v1/chat/completions`）
> - 本页为 **MiniMax 共用合同**；换模型只改 body 的 `model`
> - 默认同步；`stream: true` 流式

## 可用 model

| model id |
| --- |
| `minimax-m2.5` |
| `minimax-m2.7` |
| `minimax-m3` |


Base URL：`https://api.omnimux.ai`
