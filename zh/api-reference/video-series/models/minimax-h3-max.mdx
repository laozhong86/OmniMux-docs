---
title: "MiniMax H3 Max 旗舰视频生成"
sidebarTitle: "MiniMax H3 Max"
description: "MiniMax H3 Max 旗舰多模态视频生成 · model `minimax/h3-max`"
api: "POST https://api.omnimux.ai/v1/video/generations"
---

- 创建：`POST /v1/video/generations`，`model` 为 `minimax/h3-max`
- 动态多模态分发：传入纯文本自动走文生视频，传入 `image_url` 自动走图生视频，传入 `video_url` 自动走参考生视频
- 异步任务：用返回的 `task_id` 查询 [查询视频任务](/zh/api-reference/tasks/video-task) 或 `GET /v1/tasks/{task_id}`
- 计费：基础 5 秒单次约 $0.50（5 积分），传入 `duration: 10` 时自动结算 2.0x 倍率

## 身份

| 字段 | 值 |
| --- | --- |
| 系列 | 视频系列 |
| 品牌 | MiniMax |
| model | `minimax/h3-max` |
| 供应商后端 | fal.ai Queue |

## 接口

| 方法 | 路径 |
| --- | --- |
| `POST` | `/v1/video/generations` |
| `GET` | `/v1/tasks/{task_id}` |

Base URL：`https://api.omnimux.ai`

## 鉴权

| 名称 | 位置 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
| `Authorization` | header | string | 是 | `Bearer sk-...`（API Key） |

## 请求体

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `model` | string | 是 | 固定为 `minimax/h3-max` |
| `prompt` | string | 条件 | 画面运动与场景描述（文生、参考生必填） |
| `image_url` | string | 条件 | 图生视频首帧图片公网 URL（传入自动切换图生端点） |
| `video_url` | string | 条件 | 动作参考视频 URL（传入自动切换参考生端点） |
| `duration` | number/string | 否 | 时长（支持 `5` 或 `10`，默认 `5`；10 秒结算 2.0x 倍率） |
| `aspect_ratio` | string | 否 | 画幅宽高比，支持 `"16:9"`, `"9:16"`, `"1:1"`，默认 `"16:9"` |
| `prompt_optimizer` | boolean | 否 | 是否启用上游提示词运镜增强扩写，默认 `true` |

## 响应

### 200（创建成功）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `task_id` | string | 异步任务 ID，用于状态轮询与结果拉取 |
| `status` | string | 任务初始状态，固定为 `queued` |
| `created` | number | 任务创建时间戳 |

轮询字段见 [查询视频任务](/zh/api-reference/tasks/video-task)。错误见右栏与 [错误码](/zh/faqs/connection-usage)。

<Panel>

<RequestExample>

```bash cURL (文生视频)
curl --request POST \
  --url https://api.omnimux.ai/v1/video/generations \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
  "model": "minimax/h3-max",
  "prompt": "A beautiful cinematic view of Tokyo at night with neon lights and light rain, slow pan, photorealistic 4k",
  "aspect_ratio": "16:9",
  "duration": 5
}'
```

```bash cURL (图生视频)
curl --request POST \
  --url https://api.omnimux.ai/v1/video/generations \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
  "model": "minimax/h3-max",
  "prompt": "Camera slowly rotates around the model in natural sunlight",
  "image_url": "https://example.com/product.png",
  "duration": 5
}'
```

</RequestExample>

<ResponseExample>

```json 200
{
  "task_id": "task_v_fal_01a08124",
  "status": "queued",
  "created": 1788864000
}
```

```json 400
{
  "error": { "message": "Invalid request", "type": "invalid_request_error", "code": "bad_request" }
}
```

```json 401
{
  "error": { "message": "Invalid token", "type": "authentication_error", "code": "unauthorized" }
}
```

```json 402
{
  "error": {
    "message": "Insufficient quota. Please top up your account.",
    "type": "insufficient_quota",
    "code": "insufficient_quota"
  }
}
```

```json 429
{
  "error": { "message": "Rate limit exceeded", "type": "rate_limit_error", "code": "rate_limit_exceeded" }
}
```

```json 500
{
  "error": { "message": "Internal server error", "type": "server_error", "code": "internal_error" }
}
```

</ResponseExample>

</Panel>
