---
title: "DeepSeek · 完整参数"
sidebarTitle: "DeepSeek · 完整参数"
description: "DeepSeek · Chat Completions 完整参数（同合同，model 枚举）"
openapi: "openapi/ops/chat/deepseek.json POST /v1/chat/completions"
---

> - 协议：OpenAI Chat Completions（`POST /v1/chat/completions`）
> - 本页为 **DeepSeek 共用合同**；换模型只改 body 的 `model`
> - 默认同步；`stream: true` 流式

## 可用 model

| model id |
| --- |
| `deepseek-v4-flash` |
| `deepseek-v4-pro` |


Base URL：`https://api.omnimux.ai`
