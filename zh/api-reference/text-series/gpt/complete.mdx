---
title: "GPT · 完整参数"
sidebarTitle: "GPT · 完整参数"
description: "GPT · Chat Completions 完整参数（同合同，model 枚举）"
openapi: "openapi/ops/chat/gpt.json POST /v1/chat/completions"
---

> - 协议：OpenAI Chat Completions（`POST /v1/chat/completions`）
> - 本页为 **GPT 共用合同**；换模型只改 body 的 `model`
> - 默认同步；`stream: true` 流式

## 可用 model

| model id |
| --- |
| `gpt-5.4-mini` |
| `gpt-5.4` |
| `gpt-5.5` |
| `gpt-5.6-luna` |
| `gpt-5.6-sol` |
| `gpt-5.6-terra` |


Base URL：`https://api.omnimux.ai`
