---
title: "Upload File Stream"
sidebarTitle: "Stream Upload"
description: "POST /api/v1/files/upload/stream · Upload local files via multipart/form-data to R2"
api: "POST https://api.omnimux.ai/api/v1/files/upload/stream"
---

> - Upload local files or raw binary data via `multipart/form-data`
> - Optimized for large file streams (supports up to **100MB**)
> - Files automatically expire and are purged after **72 hours**
> - Limit of **2,000** active files per user; returns 429 when quota is exhausted
> - Auth: `Authorization: Bearer sk-...`

## Form Fields (multipart/form-data)

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `file` | binary | Yes | File binary data stream |
| `upload_path` | string | No | Custom folder path (supports camelCase `uploadPath`) |
| `file_name` | string | No | Custom file name (supports camelCase `fileName`) |

## Response Example

```json
{
  "success": true,
  "code": 200,
  "msg": "文件上传成功",
  "data": {
    "file_id": "file_349a227eaa054bab",
    "file_name": "photo.png",
    "original_name": "photo.png",
    "file_size": 2048,
    "mime_type": "image/png",
    "upload_path": "photos",
    "file_url": "https://api.omnimux.ai/api/v1/files/download/file_349a227eaa054bab",
    "download_url": "https://api.omnimux.ai/api/v1/files/download/file_349a227eaa054bab",
    "upload_time": "2026-09-10T23:49:59+08:00",
    "expires_at": "2026-09-13T23:49:59+08:00"
  }
}
```

## Using the returned URL

Use `file_url` or `download_url` from the **response** to access the uploaded file; do not reconstruct a storage URL. See [download isolation](/en/api-reference/file-series/download#uploaded-content-isolation-after-upgrade) for upgrade requirements. After the isolation update is deployed, unsafe or unverifiable public-storage settings reject uploads before a storage write.

## Request limits after upgrade

These limits apply after the upload request-limit update is deployed. Documentation publication does not confirm gateway production deployment. 1 MiB = 1,048,576 bytes. The file limit remains **100 MiB**. A stricter gateway or proxy limit still takes precedence; in particular, the existing global limit on the `/v1/files/upload/` aliases may reject a large Base64 request before it reaches this limit.

The multipart request budget is **101 MiB** (100 MiB file + 1 MiB overhead). It covers bytes consumed during parsing, including extra file parts, form fields, and multipart boundaries. The selected file must not exceed 100 MiB. An over-limit declared body, parsing that exceeds the budget, or an over-limit selected file returns **HTTP 413**. A malformed form remains **HTTP 400**.
