---
title: "Get video task"
sidebarTitle: "Get video task"
description: "GET /v1/video/generations/{task_id} · poll video generation task"
api: "GET https://api.omnimux.ai/v1/video/generations/{task_id}"
---

> - For `task_id` from **video series** create (`POST /v1/video/generations`)
> - Not the OpenAI-compatible `/v1/videos/*` family
> - Statuses: `queued` / `in_progress` / `completed` / `failed` (see live response)

Base URL: `https://api.omnimux.ai` · Auth: `Authorization: Bearer sk-...`
