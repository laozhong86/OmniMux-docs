---
title: "Create Post"
sidebarTitle: "Create Post"
description: "Create Post — explicit account source"
openapi: "openapi/ops/publishing/create-post.json POST /api/social/v1/posts"
---

All target accounts must match the requested source. Idempotency replay also validates the stored source and account set; source or set conflicts return 409. Media presign still depends on Zernio and is not an official-only upload path.

`provider` is required: `tiktok_direct` or `zernio`. It constrains the operation and never rewrites the stored source. Missing or unknown values return 400 `invalid-provider`.

Use an OmniMux user access token and `New-Api-User`, not an `sk-` key. Inspect `success` as well as HTTP status; some upstream failures return HTTP 200 with `success:false`.
