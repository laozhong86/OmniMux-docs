---
title: "Jimeng"
description: "Jimeng · Video series brand guide; shared gateway protocols and model field."
---

Brand：**Jimeng**（Jimeng）

Jimeng-native path. Availability follows channels and `GET /v1/models`.

## Recommended protocols

| Method | Path | Notes |
| --- | --- | --- |
| `POST` | `/jimeng/` | Jimeng-native format |
| `POST` | `/v1/video/generations` | Unified entry (when mapped) |

## Example models

This brand is path/channel-oriented; list model ids with `GET /v1/models`.

## Example call

```bash
curl -sS https://api.omnimux.ai/jimeng/ \
  -H "Authorization: Bearer $OMNIMUX_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "YOUR_MODEL",
    "prompt": "slow pan across a product"
  }'
```

## Related

- [Series overview](/en/api-reference/video-series/overview)
- Sibling **Protocol endpoints** in the sidebar
- [Async tasks](/en/api-reference/cross-cutting/async-tasks)
- [Models](/en/guides/models)

